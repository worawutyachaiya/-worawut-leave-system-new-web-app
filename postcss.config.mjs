/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    'tailwindcss/nesting': {}, // add new by Template
    tailwindcss: {},
    autoprefixer: {} // add new by Template
  }
};

export default config;
